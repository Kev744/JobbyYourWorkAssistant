Follow the following tasks in that order : 



1\. On src\\components\\resume-editor-panel.tsx , remove "Extraire les sections" and apply the mechanism

&#x20; 'extractSections()' on "Générer le profil" directly.

2\. Remove buttons "Exporter DOCX" and "Exporter PDF" with their associated functions.

3\. Let RichText on 'textarea' which can serve as emphasis, title or differenciation.

4\. User can export his corpus in Markdown or native text file.

5\. Importing a resume is not mandatory because he can paste his own corpus instead of resume.

6\. Guide the user by adding some tooltips to upload a resume if any and after to improve the corpus or to add directly a corpus. Say him that he can change those sections on the generated profile.

7\. Increase the corpus width.

8\. Multiple final corpus from resume or initial corpus can be saved.

9\. From the final corpus, on 'src/components/profile-form.tsx', you can select different version based on different profession or users (names). If another user, create an another workspace.

